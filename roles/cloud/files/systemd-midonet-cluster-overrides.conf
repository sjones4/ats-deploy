# Ensure network up and configuration exists

[Unit]
After=cassandra.service
After=network-online.target
Wants=network-online.target
ConditionPathExists=/etc/sysconfig/midonet-cluster
ConditionPathIsDirectory=/var/log/midonet-cluster
